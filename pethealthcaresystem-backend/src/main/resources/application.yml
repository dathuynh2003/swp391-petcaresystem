server:
  port: 8080
  servlet:
    session:
      timeout: -1
      cookie:
        name: JSESSIONID

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/pethealthcare"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true